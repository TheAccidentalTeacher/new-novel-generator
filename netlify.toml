[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  targetPort = 5173
  functions = "netlify/functions"

[functions]
  # Increase timeout for AI generation functions
  timeout = 300
